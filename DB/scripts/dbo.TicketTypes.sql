IF NOT EXISTS (SELECT * FROM sys.objects WHERE object_id = OBJECT_ID(N'[dbo].[TicketTypes]') AND type in (N'U'))
BEGIN
CREATE TABLE [dbo].[TicketTypes] (
    [ID]   INT          NOT NULL,
    [Name] VARCHAR (50) NOT NULL,
    PRIMARY KEY CLUSTERED
(
	[ID] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON, OPTIMIZE_FOR_SEQUENTIAL_KEY = OFF) ON [PRIMARY]
) ON [PRIMARY]
END

IF COL_LENGTH('TicketTypes', 'ID') IS NULL
BEGIN
	ALTER TABLE TicketTypes ADD ID INT NOT NULL
END


IF COL_LENGTH('TicketTypes', 'Name') IS NULL
BEGIN
	ALTER TABLE TicketTypes ADD Name VARCHAR(50) NOT NULL
END
